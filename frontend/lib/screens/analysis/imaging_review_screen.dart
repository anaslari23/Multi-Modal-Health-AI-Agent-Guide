import 'package:flutter/material.dart';

class ImagingReviewDetailScreen extends StatelessWidget {
  const ImagingReviewDetailScreen({super.key});

  static const routeName = '/analysis/imaging-review';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Imaging Review (placeholder)')),
    );
  }
}
