import 'package:flutter/material.dart';

class RagContextScreen extends StatelessWidget {
  const RagContextScreen({super.key});

  static const routeName = '/analysis/rag-context';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('RAG Context (placeholder)')),
    );
  }
}
