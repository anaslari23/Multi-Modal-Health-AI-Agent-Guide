import 'package:flutter/material.dart';

class FollowupQuestionsScreen extends StatelessWidget {
  const FollowupQuestionsScreen({super.key});

  static const routeName = '/intake/followup';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Follow-up Questions (placeholder)'),
      ),
    );
  }
}

