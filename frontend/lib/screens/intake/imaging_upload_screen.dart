import 'package:flutter/material.dart';

class ImagingUploadScreen extends StatelessWidget {
  const ImagingUploadScreen({super.key});

  static const routeName = '/intake/imaging-upload';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Imaging Upload (placeholder)')),
    );
  }
}
