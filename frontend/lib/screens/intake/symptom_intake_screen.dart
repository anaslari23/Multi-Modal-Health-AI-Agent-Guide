import 'package:flutter/material.dart';

class SymptomIntakeScreen extends StatelessWidget {
  const SymptomIntakeScreen({super.key});

  static const routeName = '/intake/symptoms';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Symptom Intake (placeholder)')),
    );
  }
}
