import 'package:flutter/material.dart';

class LabsUploadScreen extends StatelessWidget {
  const LabsUploadScreen({super.key});

  static const routeName = '/intake/labs-upload';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Labs Upload (placeholder)')),
    );
  }
}
